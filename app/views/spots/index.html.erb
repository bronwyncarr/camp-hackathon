<%= render partial: "shared/nav" %>
<h2>Favourite Spots</h2>
<div class ='spots-containter'>
  <% if @spots %>
    <% @spots.each do |spot| %>
      <div class="card" style="width: 18rem;">
        <% if spot.photo.attached? == true %>
          <%= image_tag spot.photo, class: "card-img-top" %>
        <% end %>
        <div class="card-body">
          <h5 class="card-title"><%= spot.name %></h5>
          <%= render 'shared/address', spot: spot %>
          <div class="icons-button">
            <div class="icons">
              <% if spot.has_power %> 🔌 <% end %>
              <% if spot.has_water %> 🚰 <% end %>
              <% if spot.has_toilet %> 🚽 <% end %>
              <% if spot.has_shower %> 🚿 <% end %>
            </div>
            <%= link_to "Details", spot, class: "btn btn-secondary" %>
          </div>
        </div>
      </div>
    <% end %>
  <% else %>
    <h4>You have no camping spots added to your favourite list yet.</h4>
    <h4>Let's add some!</h4>
  <% end %>
</div>  
